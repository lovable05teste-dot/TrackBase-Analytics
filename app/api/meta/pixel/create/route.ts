import {and,eq} from "drizzle-orm";
import {ensureDb,getDb} from "@/db";
import {metaAccounts,projects} from "@/db/schema";
import {metaConfig,metaJson} from "@/lib/meta";
import {decryptSecret,requestUserId,sha256} from "@/lib/trackbase-security";
export async function POST(request:Request){try{const userId=await requestUserId(request);if(!userId)return Response.json({error:"Não autenticado."},{status:401});const body=await request.json() as {projectId?:string;businessId?:string;name?:string;accountId?:string};if(!body.projectId||!body.businessId?.match(/^\d+$/)||!body.name?.trim())return Response.json({error:"Projeto, ID do negócio e nome do Pixel são obrigatórios."},{status:400});await ensureDb();const db=getDb(),workspaceId="ws_"+(await sha256(userId)).slice(0,24);const[project]=await db.select({id:projects.id}).from(projects).where(and(eq(projects.id,body.projectId),eq(projects.workspaceId,workspaceId))).limit(1);if(!project)return Response.json({error:"Projeto não encontrado."},{status:404});let query=db.select().from(metaAccounts).where(and(eq(metaAccounts.userId,userId),eq(metaAccounts.selected,true)));const accounts=await query;if(!accounts.length)return Response.json({error:"Vincule uma conta Meta primeiro."},{status:400});const account=accounts.find(a=>a.id===body.accountId)||accounts[0],token=await decryptSecret(account.accessTokenCipher,account.accessTokenIv),config=metaConfig();const form=new URLSearchParams({name:body.name.trim(),access_token:token});const result=await metaJson<{id:string}>(`https://graph.facebook.com/${config.version}/${body.businessId}/adspixels`,{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:form.toString()});return Response.json({pixelId:result.id,message:"Pixel criado. Gere o token CAPI no Gerenciador de Eventos e conecte abaixo."},{status:201})}catch(e){console.error("Meta pixel create",e);return Response.json({error:e instanceof Error?e.message:"A Meta não permitiu criar o Pixel."},{status:400})}}
