"use client";
import {useState} from "react";
import {ArrowLeft,Check,Copy,FileText,Megaphone,Radio,PlugZap} from "lucide-react";
import {Button} from "@/components/ui/button";
import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card";
import {Tabs,TabsContent,TabsList,TabsTrigger} from "@/components/ui/tabs";
import {MetaAccountsClient} from "../contas-meta/accounts-client";
import {TrackingInstall} from "./tracking-install";
import {MobileMenu} from "@/components/MobileMenu";
import {GatewayConnect,PixelConnect} from "./connections";

const metaUtm="utm_source=facebook&utm_medium=paid&utm_campaign={{campaign.name}}|{{campaign.id}}&utm_content={{ad.name}}|{{ad.id}}&utm_term={{adset.name}}|{{adset.id}}&placement={{placement}}&site_source_name={{site_source_name}}";

export default function IntegrationSetup(){const[copied,setCopied]=useState(false);async function copyUtm(){await navigator.clipboard.writeText(metaUtm);setCopied(true);setTimeout(()=>setCopied(false),1800)}return <main className="min-h-screen bg-slate-50 px-4 py-6 text-slate-900 sm:px-6 lg:px-10"><div className="mx-auto max-w-[1500px]">
<div className="mb-4 lg:hidden"><MobileMenu/></div>
<div className="mb-6 flex flex-wrap items-center justify-between gap-3"><a href="/" className="inline-flex items-center gap-2 text-sm text-slate-600 transition hover:text-blue-600"><ArrowLeft className="size-4"/>Voltar ao dashboard</a><div className="flex items-center gap-2"><a href="/docs" className="inline-flex items-center gap-1.5 rounded-full border border-slate-200 px-3 py-1.5 text-xs font-medium text-slate-600 transition hover:border-blue-300 hover:text-blue-600"><FileText className="size-3.5"/>Documentação dos gateways</a><span className="rounded-full border border-emerald-400/20 bg-emerald-400/10 px-3 py-1.5 text-xs text-emerald-700">Ambiente conectado</span></div></div>
<div className="mb-8 flex items-center gap-4 rounded-2xl border border-slate-200 bg-slate-50 p-5"><img src="/ghostscale-logo.png" alt="Logo GhostScale" className="h-12 w-auto shrink-0 object-contain"/><div><div><p className="text-xl font-extrabold leading-none tracking-tight text-slate-900">GhostScale</p><p className="text-xs font-medium uppercase tracking-[.18em] text-blue-600/80">Configuração</p><h1 className="mt-1 text-2xl font-semibold">Integrações</h1><p className="mt-1 text-sm text-slate-500">Conecte anúncios, rastreamento, Pixel e gateways de vendas.</p></div></div></div>
<Tabs defaultValue="ads" className="w-full"><TabsList className="mb-5 grid h-auto w-full grid-cols-2 rounded-xl border border-slate-200 bg-white p-1 sm:h-14 sm:grid-cols-4"><TabsTrigger value="ads" className="h-11 min-w-0 gap-1.5 px-2 text-xs sm:h-full sm:gap-2 sm:text-sm"><Megaphone className="size-4 shrink-0"/>Anúncios</TabsTrigger><TabsTrigger value="utms" className="h-11 min-w-0 gap-1.5 px-2 text-xs sm:h-full sm:gap-2 sm:text-sm"><FileText className="size-4 shrink-0"/>UTMs</TabsTrigger><TabsTrigger value="pixel" className="h-11 min-w-0 gap-1.5 px-2 text-xs sm:h-full sm:gap-2 sm:text-sm"><Radio className="size-4 shrink-0"/>Pixel & CAPI</TabsTrigger><TabsTrigger value="gateways" className="h-11 min-w-0 gap-1.5 px-2 text-xs sm:h-full sm:gap-2 sm:text-sm"><PlugZap className="size-4 shrink-0"/>Gateways</TabsTrigger></TabsList>
<TabsContent value="ads"><Card className="metric-card"><CardHeader><CardTitle>Contas do Gerenciador de Anúncios</CardTitle></CardHeader><CardContent><p className="mb-5 text-sm leading-6 text-slate-600">Entre com a Meta, escolha a conta que será vinculada e use “Conectar outra conta Meta” para adicionar outros logins.</p><MetaAccountsClient/></CardContent></Card></TabsContent>
<TabsContent value="utms"><Card className="metric-card"><CardHeader><CardTitle>UTMs e script da página</CardTitle></CardHeader><CardContent className="space-y-5"><p className="text-sm leading-6 text-slate-600">Copie e cole no campo <b className="text-slate-700">Parâmetros de URL</b> do anúncio. A Meta preencherá campanha, conjunto, anúncio e posicionamento automaticamente.</p><div className="rounded-xl border border-violet-500/20 bg-slate-50 p-4"><code className="block break-all text-sm leading-7 text-blue-600">{metaUtm}</code></div><Button onClick={copyUtm} className="min-w-44">{copied?<><Check/>Copiado</>:<><Copy/>Copiar UTMs</>}</Button><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[["utm_campaign","Campanha + ID"],["utm_term","Conjunto + ID"],["utm_content","Anúncio + ID"],["placement","Posicionamento"],["site_source_name","Facebook ou Instagram"],["utm_medium","Tráfego pago"]].map(([key,value])=><div key={key} className="rounded-lg border border-slate-200 bg-slate-50 p-3"><code className="text-xs text-blue-600">{key}</code><p className="mt-1 text-sm text-slate-500">{value}</p></div>)}</div><TrackingInstall/></CardContent></Card></TabsContent>
<TabsContent value="pixel"><PixelConnect/></TabsContent><TabsContent value="gateways"><GatewayConnect/></TabsContent>
</Tabs></div></main>}
