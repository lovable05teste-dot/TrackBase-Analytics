"use client";
import {FormEvent,useState} from "react";
import {Button} from "@/components/ui/button";
import {Input} from "@/components/ui/input";

export default function Login(){const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});const b=await r.json();if(!r.ok)throw new Error(b.error);location.href="/";}catch(e){setError(e instanceof Error?e.message:"Não foi possível entrar.");}finally{setLoading(false)}}
return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#dbeafe_0%,#f8fafc_45%)] p-5 text-slate-900"><form onSubmit={submit} className="metric-card w-full max-w-sm space-y-6 rounded-2xl border border-slate-200 p-8 shadow-2xl shadow-violet-950/30"><div><div className="flex items-center gap-2 font-bold text-blue-600"><img src="/trackbase-logo.png" alt="Logo TrackBase Analytics" className="h-10 w-20 object-contain"/>TrackBase Analytics</div><div className="mt-5"><p className="text-xs font-medium uppercase tracking-[.18em] text-blue-600/80">Área segura</p><h1 className="mt-2 text-2xl font-semibold">Entrar no workspace</h1></div><p className="mt-2 text-sm leading-6 text-slate-600">Acesse seu painel de vendas, atribuição e integrações.</p></div><Input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" autoFocus required/>{error&&<p className="text-sm text-red-600">{error}</p>}<Button className="h-11 w-full" disabled={loading}>{loading?"Validando acesso…":"Entrar no painel"}</Button><p className="text-center text-xs text-slate-500">Sua sessão é protegida por cookie seguro.</p></form></main>}
